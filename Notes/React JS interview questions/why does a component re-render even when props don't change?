This is a **very common React confusion**, and the key is understanding **what actually triggers a re‑render** versus **what *prevents* one**.

I’ll explain it **clearly, precisely, and practically**, with **why it happens**, **what React checks**, and **how to control it**.

***

# ✅ Why does a React component re‑render **even when props don’t change**?

## 🔑 Short, precise answer (interview‑ready)

> A component re‑renders when **its parent re‑renders**, **its state changes**, or **its context changes**, even if the props *appear* unchanged.

React re‑renders by **default behavior**, not by comparing props deeply.

***

## 🧠 The MOST IMPORTANT RULE (memorize this)

> **React re‑renders a child whenever its parent re‑renders — unless you explicitly prevent it.**

This alone explains **90% of cases**.

***

# 🔁 1. Parent re‑render → Child re‑render (DEFAULT behavior)

### ❗ Even if props are the same **by value**

```jsx
function Parent() {
  const [count, setCount] = useState(0);

  return (
    <>
      <button onClick={() => setCount(c => c + 1)}>+</button>
      <Child name="Raja" />
    </>
  );
}
```

👉 Clicking the button:

*   `Parent` state changes ✅
*   `Parent` re‑renders ✅
*   `Child` re‑renders ✅  
    **even though `name="Raja"` didn’t change**

### ✅ Why?

React does **not assume** child output is safe to reuse.

> Re‑render ≠ DOM update  
> React **recalculates**, then decides what to update.

***

# 🔁 2. Props LOOK the same, but are **new references**

This is the **#1 interview trick**.

### Example

```jsx
<Child data={{ id: 1 }} />
```

Every render creates a new object:

```js
{ id: 1 } !== { id: 1 }
```

✅ Value is same  
❌ Reference is different

React compares **by reference**, not deep equality.

***

### Same problem with functions

```jsx
<Child onClick={() => doSomething()} />
```

Every render:

*   New function
*   New reference
*   Child re‑renders

***

# 🔁 3. State changes inside the component

Even with unchanged props:

```jsx
setState(...)
```

➡️ Component **must re‑render**

This is expected and correct.

***

# 🔁 4. Context changes

If a component uses context:

```jsx
const theme = useContext(ThemeContext);
```

Even when props stay the same:

*   Context value changes
*   Component re‑renders

***

# 🔁 5. `React.StrictMode` (development only)

In development:

*   React **intentionally re‑renders twice**
*   Helps find side effects

This does **not happen in production**.

***

# 🧠 Key Clarification (very important)

## ❌ Re‑render does **NOT** mean:

*   DOM updates
*   Performance is bad

## ✅ Re‑render means:

*   Function runs again
*   React compares virtual DOM
*   Minimal DOM updates happen

React is designed for this.

***

# ✅ How to PREVENT unnecessary re‑renders

You must **opt‑in** to preventing them.

***

## ✅ 1. `React.memo` (for props)

```jsx
const Child = React.memo(function Child({ name }) {
  console.log("Child render");
  return <div>{name}</div>;
});
```

Now:

*   Parent re‑renders ✅
*   Child re‑renders ❌ (if props unchanged)

✅ React.memo does a **shallow comparison**

***

## ⚠️ Important: memo does NOT help if references change

```jsx
<Child data={{ id: 1 }} />  // ❌ still re‑renders
```

***

## ✅ 2. `useCallback` (stabilize functions)

```jsx
const handleClick = useCallback(() => {
  doSomething();
}, []);
```

Prevents:

```js
newFunction !== oldFunction
```

***

## ✅ 3. `useMemo` (stabilize objects/arrays)

```jsx
const data = useMemo(() => ({ id: 1 }), []);
```

Now reference is stable.

***

## ✅ Combined Correct Pattern

```jsx
const data = useMemo(() => ({ id: 1 }), []);
const onClick = useCallback(() => {}, []);

<Child data={data} onClick={onClick} />
```

✅ Child truly does not re‑render

***

# 📊 Summary Table

| Cause                     | Re‑render? | Why                       |
| ------------------------- | ---------- | ------------------------- |
| Parent re‑render          | ✅          | Default behavior          |
| Same primitive prop       | ✅          | Parent re‑render triggers |
| New object/function       | ✅          | Reference changed         |
| State change              | ✅          | Required                  |
| Context change            | ✅          | Subscription updates      |
| React.memo + stable props | ❌          | Shallow comparison        |

***

# 🧠 Correct Mental Model (MOST important)

> React components are **functions**.
> When something triggers them, they run again.
> Preventing re‑renders is **your responsibility**, not React’s default.

***

# ✅ One‑line interview answer

> A component can re‑render even when props look unchanged because its parent re‑rendered, or because props passed by reference (objects/functions) are recreated, causing shallow comparison to fail.

***

# ✅ 10‑second interview explanation

> React re‑renders children by default when the parent re‑renders, and because props are compared by reference, not by value. Memoization is required to prevent this.

***

# ✅ Final takeaway (say this confidently)

> **Re‑rendering is cheap; unnecessary DOM updates are expensive. React avoids the latter automatically.**

***

If you want next, I can explain:
✅ Why **re‑rendering is OK**
✅ Difference between **re‑render and DOM update**
✅ Visual diagram of React reconciliation
✅ Real debugging checklist for re‑renders

Just say 👍
