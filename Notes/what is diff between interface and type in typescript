Here’s a **clear, interview‑ready comparison of `interface` vs `type` in TypeScript**, with examples and best‑practice guidance 👇

***

## 1️⃣ What are `interface` and `type`?

Both are used to **define the shape of data** (objects, functions, etc.) and help TypeScript with **type checking**.

```ts
// interface
interface User {
  id: number;
  name: string;
}

// type
type User = {
  id: number;
  name: string;
};
```

✅ For simple objects, they look **almost identical**.

***

## 2️⃣ Key Differences at a Glance ✅

| Feature              | interface   | type                  |
| -------------------- | ----------- | --------------------- |
| Object shape         | ✅ Yes       | ✅ Yes                 |
| Extend / inheritance | ✅ `extends` | ✅ intersections (`&`) |
| Declaration merging  | ✅ Yes       | ❌ No                  |
| Union types          | ❌ No        | ✅ Yes                 |
| Tuple support        | ❌ No        | ✅ Yes                 |
| Primitive types      | ❌ No        | ✅ Yes                 |
| Recommended for APIs | ✅ Yes       | ❌ Usually not         |
| Flexibility          | Medium      | High                  |

***

## 3️⃣ Declaration Merging (BIG Difference) ⭐

### ✅ `interface` supports declaration merging

```ts
interface User {
  name: string;
}

interface User {
  age: number;
}

const user: User = {
  name: "Ravi",
  age: 30
};
```

✅ TypeScript **automatically merges** them.

***

### ❌ `type` does NOT support merging

```ts
type User = {
  name: string;
};

type User = {
  age: number;
}; // ❌ Error: Duplicate identifier
```

📌 **This is why interfaces are preferred for public APIs & libraries**

***

## 4️⃣ Extending Types

### ✅ `interface` extension (cleanest)

```ts
interface Person {
  name: string;
}

interface Employee extends Person {
  id: number;
}
```

***

### ✅ `type` extension (intersection)

```ts
type Person = {
  name: string;
};

type Employee = Person & {
  id: number;
};
```

✅ Both work, but `interface` reads more naturally for object hierarchies.

***

## 5️⃣ Union Types (Only `type` ✅)

### ❌ Not possible with interface

```ts
// interface Status = "success" | "error"; ❌
```

### ✅ With type

```ts
type Status = "success" | "error" | "loading";
```

✅ This is a **major reason to use `type`**

***

## 6️⃣ `type` Can Define More Than Objects ✅

### ✅ Primitives

```ts
type ID = number | string;
```

### ✅ Tuples

```ts
type Point = [number, number];
```

### ✅ Function Types

```ts
type Add = (a: number, b: number) => number;
```

❌ `interface` cannot do these cleanly.

***

## 7️⃣ Interface for Classes (Best Practice)

```ts
interface Animal {
  eat(): void;
}

class Dog implements Animal {
  eat() {
    console.log("Eating");
  }
}
```

✅ `interface` fits perfectly with OOP-style design.

***

## 8️⃣ React Use Cases ✅

### ✅ Props & State → `interface` (Recommended)

```ts
interface Props {
  title: string;
  count: number;
}

function Component({ title, count }: Props) {}
```

### ✅ Union props → `type`

```ts
type ButtonProps =
  | { variant: "primary"; color: string }
  | { variant: "secondary"; size: number };
```

***

## 9️⃣ Performance & Tooling

✅ `interface`:

*   Slightly better error messages
*   Better editor support in large codebases
*   Designed for **extensibility**

✅ `type`:

*   More powerful
*   Better for advanced type logic

***

## 🔑 When to Use What (Rule of Thumb)

### ✅ Use `interface` when:

*   Defining object shapes
*   Designing public APIs
*   Working with classes
*   Expecting extension or merging

### ✅ Use `type` when:

*   Using unions (`|`)
*   Using tuples
*   Defining aliases for primitives
*   Doing complex type compositions

***

## ✅ Interview‑Ready Answer (Short & Strong)

> **`interface` is best for defining object shapes and supports declaration merging, while `type` is more flexible and supports unions, tuples, and primitives. Use `interface` for extensible object models and `type` for complex type logic.**

***

## ✅ One‑Line Best Practice ⭐

> **Use `interface` by default. Use `type` when you need unions or advanced typing.**

***

If you want, I can also explain:

*   ✅ `interface` vs `class`
*   ✅ Advanced `type` tricks
*   ✅ Real interview questions
*   ✅ React‑specific patterns

Just tell me 😊
